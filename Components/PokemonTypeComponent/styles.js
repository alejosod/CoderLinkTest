import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    body: {
        flex: 1,
        justifyContent: 'flex-start',
        flexWrap: 'wrap',
        flexDirection: 'row'
    }
});